import { TailSpin } from "react-loader-spinner";

const Loader : React.FC = () => (
    <TailSpin
        height="60"
        width="60"
        color="#D2A6FF"
        radius="1"
    />
)

export default Loader;
